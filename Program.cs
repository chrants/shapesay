using System;
using System.Linq;

namespace shapesay
{
    class Program
    {
        static void Main(string[] args)
        {

            ShapeSay.Say(args);

            // string name = "Thanh Nguyen";
            // foreach (var _ in Enumerable.Range(1, 100).ToArray())
            // {
            //     Console.WriteLine($"{name} is a poopoo head.");
            // }

            // foreach (var thing in args)
            // {
            //     Console.WriteLine(thing);
            // }
        }
    }
}
